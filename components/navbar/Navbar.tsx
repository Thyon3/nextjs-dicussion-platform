// Backward-compatible re-export.
// The Navbar has been migrated to the auth feature.
// All new code should import from '@/src/features/auth'.
export { default } from '@/src/features/auth/components/Navbar';
