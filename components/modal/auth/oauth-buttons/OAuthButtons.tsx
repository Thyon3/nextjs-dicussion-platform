import React from "react";

/**
 * Social authentication buttons for Google and other providers.
 * Disabled for now as the new backend only supports email/password.
 */
const OAuthButtons: React.FC = () => {
  return null;
};

export default OAuthButtons;

